namespace TcbInternetSolutions.Vulcan.Core
{
    // TODO: Research if index job can be batched similar to Find.
    // TODO: Research if possible to have unified search Find equivalent to also include pocos.

    // todo: changes, now DI friendly, IVulcanIndexers must be registered via IConfigurable module, IVulcanIndexModifier via ServiceConfiguration attribute
    // todo: net461 will need to support NEST 2x and NEST 5x will need to target net462, still need to decide which version gets main Nuget ID
}